import React from 'react';
import { Link } from 'react-router-dom';

export default function DashBord() {
  return (
    <div>
      <button>
        <Link to="Classpractice">Workspace</Link>
      </button>
    </div>
  );
}
